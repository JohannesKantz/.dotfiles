# Windows

Migrate later from `tmp/.dotfiles`:

```text
Documents/WindowsPowerShell/Microsoft.PowerShell_profile.ps1
Windows Terminal settings
WinGet settings and package lists
PowerToys settings
scripts/settings/basicWindowsSettings.ps1
```

Use PowerShell setup for native Windows. Use the regular Stow packages inside
WSL.
