# Legacy migration input. Keep it until the new setup has been tested.
/tmp/

# Local editor and operating-system files.
.DS_Store
*.swp
*.swo
*~

# SSH secrets and machine-specific hosts.
ssh/.ssh/config.local
ssh/.ssh/known_hosts*
ssh/.ssh/id_*
ssh/.ssh/*.pem
